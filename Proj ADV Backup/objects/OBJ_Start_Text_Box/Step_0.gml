//// Step Event of obj_press_start_text
//blink_timer += delta_time; // Increment timer by the time elapsed since the last frame

//// Calculate the current time within the blink cycle
//var time_in_cycle = blink_timer mod blink_interval;

//// Determine visibility: text is visible during the blink_duration at the start of each cycle
//is_visible = (time_in_cycle < blink_duration);